
const name = 'Dead End';
const realName = "Ruijerd Supardia";

const completeName = name + ' ' + realName;

console.log(completeName);

const template = `${name} ${realName}`;

console.log(template);

const html = `
    <h1>Hi</h1>
    <p>World</p>
`; //careful with line breaks

console.log(html);